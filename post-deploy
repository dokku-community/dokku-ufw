#!/usr/bin/env bash
set -eo pipefail; [[ $DOKKU_TRACE ]] && set -x
source "$PLUGIN_AVAILABLE_PATH/ports/functions"

ports-post-deploy() {
  declare APP="$1"
  fn-ports-open "$APP"
}

ports-post-deploy "$@"
